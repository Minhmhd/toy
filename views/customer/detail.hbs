<!DOCTYPE html>
<html lang="en">

<head>
   <meta charset="UTF-8">
   <meta name="viewport" content="width=device-width, initial-scale=1.0">
   <title>Toy Detail</title>
</head>
    <style>
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background-color: #f8f9fa;
            margin: 0;
        }
                .navbar {
            background-color: rgba(0, 0, 0, 0.5);
            border-radius: 0;
            padding: 10px;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            z-index: 1000; /* Ensure navbar is on top of other content */
            height: 6%;
        }
        .navbar .nav-link, .navbar-brand {
            color: white;
        }
        .navbar .nav-link:hover, .navbar-brand:hover {
            color: #ffc107;
        }

        .container {
            max-width: 1200px;
        }
        .card {
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }
    </style>

<body>
   <div class="container">
      <div class="row">
         <div class="col">
            <img src="{{ toy.cover }}" width="400" height="450">
         </div>
         <div class="col">
            <h1 class="text text-danger">toy Detail</h1>
            <h3>Name: {{ toy.name }}</h3>
            <h3>Origin: {{  toy.origin.country }}</h3>
            <h3>Description: {{ toy.description }}</h3>
            <h3>Price: ${{ toy.price }}</h3>
            <a class="btn btn-success" href="/login">Add to cart</a>
         </div>
         
      </div>
   </div>
</body>

</html>