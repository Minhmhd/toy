<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <style>
        body {
            background-image: url('https://www.kawanlamagroup.com/storage/987/Toys-Kingdom-Web-%281%29.jpg');
            background-size: cover;
            background-repeat: no-repeat;
            background-attachment: fixed;
            color: white;
            margin: 0;
            padding: 0;
            font-family: Arial, sans-serif;
        }

        .navbar {
            background-color: rgba(0, 0, 0, 0.5);
            border-radius: 0;
            padding: 10px;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            z-index: 1000;
            height: 6%;
        }

        .navbar .nav-link,
        .navbar-brand {
            color: white;
        }

        .navbar .nav-link:hover,
        .navbar-brand:hover {
            color: #ffc107;
        }

        .sidebar {
            background-color: rgba(0, 0, 0, 0.5);
            border-radius: 10px;
            padding: 10px;
            position: fixed;
            top: 70px;
            left: 0;
            width: 9%;
            height: calc(100% - 70px);
            overflow-y: auto;
        }

        .content {
            margin-left: 220px;
            margin-top: 70px;
        }

        .footer {
            background-color: rgba(0, 0, 0, 0.5);
            padding: 5px;
            text-align: center;
            position: fixed;
            bottom: 0;
            width: 100%;
            height: 4%;
        }

        .footer .text-center {
            font-size: 0.8rem;
        }

        .search-bar {
            width: 400px;
            display: flex;
            align-items: center;
        }

        .search-bar input[type="search"] {
            flex: 1;
            border: none;
            padding: 10px;
            border-radius: 5px 0 0 5px;
            outline: none;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        .search-bar button {
            border: none;
            padding: 10px 20px;
            border-radius: 0 5px 5px 0;
            background-color: #ffc107;
            color: white;
            cursor: pointer;
            outline: none;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        .search-bar button:hover {
            background-color: #e0a800;
        }

        .navbar .navbar-nav {
            margin-left: auto;
        }
    </style>
</head>

<body>
    <nav class="navbar navbar-expand-lg navbar-light">
        <div class="container-fluid">
            <a class="navbar-brand" href="/">Home</a>
            <div class="collapse navbar-collapse justify-content-center" id="navbarNav">
                <form class="search-bar mx-auto" action="/search" method="post">
                    <input type="search" placeholder="Search by name toy" aria-label="Search" name="name">
                    <button type="submit">Search</button>
                </form>
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link" href="/about">About Us</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/contact">Contact Us</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/login">Login</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/signup">Sign Up</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <div class="sidebar">
        <h5>Categories</h5>
        <ul class="nav flex-column">
            {{#each categories}}
            <li class="nav-item">
                <a class="nav-link" href="/customer/toy/{{this._id}}">{{this.name}}</a>
            </li>
            {{/each}}
        </ul>
    </div>

    <div class="content container mt-4">
        {{{body}}}
    </div>

    <footer class="footer bg-light text-center text-lg-start mt-auto">
        <div class="text-center p-3">
            &copy; 2024 Toy Kingdoms
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-kenU1KFdBIe4zVF0s0G1M5b4hcpxyD9F7jL+GlB1BxZCf5C6V4COm9n5paUefb1a" crossorigin="anonymous">
    </script>
</body>

</html>